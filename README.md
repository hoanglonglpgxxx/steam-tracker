crontab check mỗi đầu giờ (từ 6h - 22h): query DB filter ngày hôm đó + mốc thời gian đó, có bản ghi thì gọi hàm sendEmail vào thời gian của bản ghi đó


WHAT IS Promise.all?


các lệnh cần chạy ở máy nhà
git remote add gitlab  https://gitlab.com/hoanglonglpgxxx/steam-tracker.git
git pull gitlab main
git remote rename origin github-backup
git remote rename gitlab origin
git push -u origin main

